import { Link } from "react-router-dom";
import Button from "./Button";

const NavBar = () => {
  return (
    <nav>
      <ul>
        <li></li>
      </ul>
    </nav>
  );
};

export default NavBar;
